{
  "version": "0.0.0",
  "private": true,
  "name": "askkit-app",
  "description": "A polling system app",
  "repository": "https://github.com/sergiorb/askkit-app",
  "license": "",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "./scripts/postinstall.sh",

    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8080"
  }
}
